services:
  postgresql:
    image: postgres:16
    hostname: postgresql
    container_name: postgresql
    environment:
      POSTGRES_DB: "conduktor-console"
      POSTGRES_USER: "conduktor"
      POSTGRES_PASSWORD: "passwd123"
      POSTGRES_HOST_AUTH_METHOD: "scram-sha-256"
  conduktor-console:
    image: conduktor/conduktor-console:1.35.2
    hostname: conduktor-console
    container_name: conduktor-console
    depends_on:
      - postgresql
    ports:
      - "8083:8080"
    volumes:
      - type: bind
        source: "./configs/platform-config.yml"
        target: /opt/conduktor/platform-config.yaml
        read_only: true
    environment:
      CDK_IN_CONF_FILE: /opt/conduktor/platform-config.yaml
        #  conduktor-monitoring:
        #    hostname: conduktor-monitoring
        #    container_name: conduktor-monitoring
        #    image: conduktor/conduktor-console-cortex:1.35.2
        #    environment:
        #      CDK_CONSOLE-URL: "http://conduktor-console:8083"
